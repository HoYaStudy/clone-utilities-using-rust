# Clone Utilities Using Rust

This project is to clone GNU utilities using Rust

## Utility List

* `true` / `false`
* `echo`
  * -n
* `cat`
  * -n, -b
* `head`
  * -n, -c
* `wc`
  * -l, -w, -c, -m
* `uniq`
  * -c
* `find`
  * -n, -t
